windows:
  - name: "lsd.vm"
    clear: false
    options:
      synchronize-panes: false
    root: "~/dev/projects/lsd/impl"
    filters:
      before:
        - "source ~/.bash_profile"
    splits:
      - cmd: ["ssh root@192.168.56.2"]
      - cmd: ["ssh root@192.168.56.2"]
        height: 50
  - name: "lsd"
    clear: false
    options:
      synchronize-panes: false
    root: "~/dev/projects/lsd/impl"
    filters:
      before:
        - "source ~/.bash_profile"
    splits:
      - cmd: "pwd"
